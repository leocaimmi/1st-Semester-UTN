#include <stdio.h>
#include <stdlib.h>
#include "pila.h"
///Hacer una función que permita ingresar varios elementos a una pila, tanto como quiera el usuario.

int cargarPila(Pila(&a));


int main()
{
int cargarPilita
cargarPila = (int cargarPilita);



    return 0;
}

int cargarPila(Pila(&a))
{
inicpila(&a);
char continuar;
    do
    {

        leer(a)
        printf("c para seguir cargando elementos, x para finalizar");
        fflush(stdin);
        scanf("%c",&continuar);


    }
    while(continuar == 'c')

return continuar;


    }





